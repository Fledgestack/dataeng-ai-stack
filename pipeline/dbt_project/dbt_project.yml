name: stack
version: "1.0.0"
config-version: 2
profile: stack

model-paths: ["models"]
target-path: target
clean-targets: [target]

models:
  stack:
    +database: lake          # the attached DuckLake catalog
    staging:
      +schema: staging
      +materialized: table
    marts:
      +schema: marts
      +materialized: table
