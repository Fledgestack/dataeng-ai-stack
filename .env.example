# Copy to .env and change the password. `make init` does this for you.
POSTGRES_USER=stack
POSTGRES_PASSWORD=CHANGE_ME_long_random_string

# Kill switch: set to false and every pipeline run fails before touching data.
PIPELINE_ENABLED=true
